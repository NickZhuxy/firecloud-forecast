name: Precompute Firecloud products

on:
  workflow_dispatch:
    inputs:
      start_date:
        description: First forecast date in YYYY-MM-DD (Shanghai today when empty)
        required: false
        type: string
      days:
        description: Number of forecast dates to publish
        required: false
        default: 2
        type: number
      locations:
        description: Comma-separated NAME:LAT:LON local products
        required: false
        default: shanghai:31.23:121.47
        type: string
      local_radius:
        description: Local product radius in km
        required: false
        default: 150
        type: number
      local_resolution:
        description: Local product evaluation-grid spacing in degrees
        required: false
        default: 0.1
        type: number
  schedule:
    # GFS runs at 00/06/12/18Z. Avoid the busy top of the hour and allow
    # upstream products to finish publishing before starting the build.
    - cron: "37 4,10,16,22 * * *"

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: firecloud-pages-precompute
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 330
    env:
      MPLBACKEND: Agg
      START_DATE: ${{ inputs.start_date || '' }}
      DAYS: ${{ inputs.days || 2 }}
      LOCATIONS: ${{ inputs.locations || 'shanghai:31.23:121.47' }}
      LOCAL_RADIUS: ${{ inputs.local_radius || 150 }}
      LOCAL_RESOLUTION: ${{ inputs.local_resolution || 0.1 }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Set up uv
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          enable-cache: true

      - name: Install runtime dependencies
        run: uv sync --frozen --no-dev

      - name: Build static forecast feed
        shell: bash
        run: |
          args=(
            --days "$DAYS"
            --output _site
            --algorithm-version "${GITHUB_SHA::12}"
            --local-radius "$LOCAL_RADIUS"
            --local-resolution "$LOCAL_RESOLUTION"
          )
          if [[ -n "$START_DATE" ]]; then
            args+=(--start-date "$START_DATE")
          fi
          IFS=',' read -ra location_specs <<< "$LOCATIONS"
          for location in "${location_specs[@]}"; do
            if [[ -n "$location" ]]; then
              args+=(--location "$location")
            fi
          done
          uv run --no-sync python -m predictor.precompute "${args[@]}"

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: _site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    timeout-minutes: 15
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
